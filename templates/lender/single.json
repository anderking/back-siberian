{
	"status": true,
	"message": {
		"code": "{{#? message.code}}",
		"text": "{{#? message.text}}"
	},
	"lender": {
		"id": "{{#? lender.id}}",
		"firstName": "{{#? lender.firstName}}",
		"lastName": "{{#? lender.lastName}}",
		"email": "{{#? lender.email}}",
	    "dniType": "{{#? lender.dniType}}",
		"dni": "{{#? lender.dni}}",
		"dateOfBirth": "{{#? lender.dateOfBirth}}",
		"imageUrl": "{{#? lender.imageUrl}}",
		"gender": "{{#? lender.gender}}",
		"note": "{{#? lender.note}}",
		"status": "{{#? lender.status}}",
		"skills": {
			"{{#each lender.Skills}}": {
				"id": "{{#? this.id}}",
				"name": "{{#? this.name}}",
				"description": "{{#? this.description}}",
				"status": "{{#? this.status}}",
				"experienceYears": "{{#? this.SkillsLenders.experienceYears}}"
			}
		}
	}
}