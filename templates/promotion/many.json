{
	"status": true,
	"message": {
		"code": "{{#? message.code}}",
		"text": "{{#? message.text}}"
	},
	"promotions": {
		"{{#each this.Promotions}}": {
			"id": "{{#? this.id}}",
			"name": "{{#? this.name}}",
			"description": "{{#? this.description}}",
			"type": "{{#? this.type}}",
			"percentDiscount": "{{#? this.percentDiscount}}",
			"dateStart": "{{#? this.dateStart}}",
			"dateEnd": "{{#? this.dateEnd}}",
			"imageUrl": "{{#? this.imageUrl}}",
			"status": "{{#? this.status}}",
			"createdAt": "{{#? this.createdAt}}",
			"updatedAt": "{{#? this.updatedAt}}",
			"equipinfras": {
				"id": "{{#? this.Equipinfra.id}}",
				"name": "{{#? this.Equipinfra.name}}",
				"type": "{{#? this.Equipinfra.type}}",
				"imageUrl": "{{#? this.Equipinfra.imageUrl}}",
				"description": "{{#? this.Equipinfra.description}}",
				"status": "{{#? this.Equipinfra.status}}"
			},
			"subcategory": {
				"id": "{{#? this.Subcategory.id}}",
				"name": "{{#? this.Subcategory.name}}",
				"description": "{{#? this.Subcategory.description}}",
				"status": "{{#? this.Subcategory.status}}"
			},
			"parameters": {
				"{{#each this.Parameters}}": {
					"id": "{{#? this.id}}",
					"name": "{{#? this.name}}",
					"description": "{{#? this.description}}",
					"status": "{{#? this.status}}"
				}
			}
		}
	}
}