{
	"status": true,
	"message": {
		"code": "{{#? message.code}}",
		"text": "{{#? message.text}}"
	},
	"notificationDetails": {
		"{{#each notificationDetails}}": {
			"id": "{{#? this.id}}",
			"name": "{{#? this.name}}",
			"message": "{{#? this.message}}",
			"status": "{{#? this.status}}",
			"createdAt": "{{#? this.createdAt}}",
			"updatedAt": "{{#? this.updatedAt}}"
		}
	}
}