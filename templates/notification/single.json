{
	"status": true,
	"message": {
		"code": "{{#? message.code}}",
		"text": "{{#? message.text}}"
	},
	"notification": {
		"id": "{{#? notification.id}}",
		"UserId": "{{#? notification.UserId}}",
		"PromotionId": "{{#? notification.PromotionId}}",
		"BitacoraId": "{{#? notification.BitacoraId}}",
		"createdAt": "{{#? notification.createdAt}}",
		"read": "{{#? notification.read}}",
		"notificationDetail": {
			"id": "{{#? NotificationDetail.id}}",
			"name": "{{#? NotificationDetail.name}}",
			"message": "{{#? NotificationDetail.message}}",
			"status": "{{#? NotificationDetail.status}}"
		}
	}
}